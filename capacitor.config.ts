import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Malistartups_Invest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
